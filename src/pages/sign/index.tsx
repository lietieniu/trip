import * as React from 'react';

interface ISignProps {
}

const Sign: React.FunctionComponent<ISignProps> = (props) => {
    return (
        <div>
            <h1>登录页面</h1>
        </div>
    );
};

export default Sign;
